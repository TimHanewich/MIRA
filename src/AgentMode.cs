using System;

namespace AIA
{
    public enum AgentMode
    {
        Autonomous = 0,   //Tell the Agent to make buy/sell decisions on its own
        Assistant = 1     //Guided experience with the user
    }
}