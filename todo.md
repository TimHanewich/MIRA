# To-Do

- Add more tools
- Upon boot, prompt with menu to select what to do:
    - Review portfolio
    - Start autonomous investing agent
- In `TimHanewich.Investing` package, 
    - change TransactedAt from Unix timestamp to DateTimeOffset (`Transaction` class)
    - Fix `PortfolioPerformance` mispelling
- In `TimHanewich.AgentFramework` package
    - Call to FunctionInvoked for server-side web searches